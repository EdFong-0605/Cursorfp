/** Top-of-column search / filter region for the landing content area. */
import './SearchBar.css';

function SearchBar() {
  return (
    <div className="searchbar-sheet" role="search">
      <span className="searchbar-sheet__title">SearchBar</span>
    </div>
  );
}

export default SearchBar;
